# 2026-08-31 工作日志

## 3D 赛璐璐桌游店模拟经营游戏（software-boardgame-shop 团队，标准 SOP）
- 全流程：PM 许清楚 PRD → 架构高见远（20 文件三层：sim 纯逻辑/scene Three.js/ui DOM）→ 工程寇豆码实现 → QA 严过关两轮验证
- 主理人裁决：营业 75 秒、轻干预、文本气泡剧情、季节升 P1（10 天换季 ±30% 热度 + 季首 3 天活动周 ×1.25）；裁音效/贷款，留存档
- QA R1 打回：restock/setPrice NaN 守卫、种子 0 的 `||`→`??`、平衡性（5-6 天通关过快 → C10 调参后 18-20 天达标，目标 18-25）
- 终验：42/42 测试全绿（economy 11 + simulation 8 + edge 23），交付完成
- 技术要点：three@0.160.0 import map CDN 免打包；node --test 无头测 sim 层；Node 24 下 `node --test tests/` 目录形式报 MODULE_NOT_FOUND，需显式文件
- 试玩：python http.server 8321 → http://localhost:8321

## 迭代 1：第一人称视角（团队 software-bgs-fp-camera，快速模式）
- 用户要求"玩家以第一人称视角参与"。寇豆码实现 + 严过关验证，一轮通过无打回
- 新增 src/scene/firstPerson.js（纯函数 + 控制器，node 可测）：Pointer Lock + WASD + AABB 分轴滑动碰撞；main.js 手势链（弹窗时禁进锁、命中顾客先退锁再弹卡）、V 键 fps/iso 切换
- 测试 42→59（+collision 11 +firstPersonWorld 6：出生点合法性 12 组合、BFS 连通性证明无死区、布局漂移守卫直接读 shop.js 常量交叉比对——好方法可复用）
- 手感参数集中 CONFIG.firstPerson；已知限制：ESC 后 1s 内重进锁定有 Chrome unhandled rejection 噪音

## 迭代 2：大型增量——场景/物流/结账/员工（团队 software-bgs-expansion，标准增量 SOP）
- 流程：PM×2（Part A 物流/SKU/结账 + Part B 员工/气泡/场景，曾因 Write 覆盖事故立"文件所有权"规则）→ 架构（25 文件/11 任务/stepAutoStock 虚拟搬运工）→ 工程×2 并行（sim 层 vs scene/UI 层，文件所有权隔离）
- 关键教训1：PM-2 用源码复核推翻主理人裁决（dailyFootfall 与 openDuration 无关，客流是按天固定总数）——裁决前先看代码；冲突治理用"例外条款+删除线留痕+主理人回签"
- 关键教训2：工程师被 429 配额打断（5h 上限 14:36 重置），主理人直接接管修复
- 修复 2 个阻断 bug：① unloadDelivery 把 d.boxes（对象数组）当 id 数组 → 订货卡 inTransit 黑洞；② takeFromShelf 取空保留 sku 绑定（qty=0,sku≠null）→ restockToSlot 找不到格 → 品类货架永久锁 0。均含回归测试守卫
- 测试脚本坑：`.map(playOneGame)` 会把数组下标当第二参数传入（maxDays=index），导致"footfall=7 悬崖"假象——写 map 回调必须显式箭头函数
- 终态：129/129 测试全绿（11 个测试文件）；通关 12-23 天（完美 bot 上限）；footfall.base=7；存档 v1→v2 一次性迁移
- 玩法闭环：MORNING→PREP 备货 90s（货车卸箱/开箱/搬货上架，货架初始空）→OPEN 105s（手动结账 2s/位或雇员工，A33 自助兜底 14s）→CLOSING；两阶段 2 倍速
